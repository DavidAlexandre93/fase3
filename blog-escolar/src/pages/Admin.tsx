import AudioRead from '../components/AudioRead';
import React from "react";

const Admin: React.FC = () => {
  // Aqui você vai buscar e exibir a lista de posts, com botões para editar/excluir
  return (
    <div>
      <h1>Painel Administrativo <AudioRead text="Painel Administrativo" /></h1>
      {/* Lista de posts com botões de editar/excluir */}
    </div>
  );
};

export default Admin;
